// Отправка нажатий стрелок в активное окно.
//
// SendInput кладёт события в общую очередь ввода, поэтому игра получает их
// ровно так же, как настоящее нажатие клавиши, и никаких особых прав не нужно.

#pragma once

#include <windows.h>

#include "bitboard.h"

namespace app {

void sendMove(bb::Move move);

// Клавиши уходят в то окно, которое сейчас в фокусе, поэтому перед отправкой
// проверяем, что доска действительно принадлежит активному окну. Иначе стоит
// переключиться на другую программу — и она получит поток стрелок.
bool boardWindowIsActive(int screenX, int screenY);

}  // namespace app
